# Rust build artefacts
target/

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

# Captured HARs and consolidator outputs are operator-side — never tracked.
# Exception: a vendored snapshot lives under tests/fixtures/ for the replay test.
*.har
pixiechess-api.json
!tests/fixtures/pixiechess-api.json
api-reference.json
api-reference.md
